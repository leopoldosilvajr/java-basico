spring:
  datasource:
    url: jdbc:postgresql://autorack.proxy.rlwy.net:19851/railway
    username: postgres
    password: KNQRTpeWCqqkRXduHoYxXxaSqDRoEyxO
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect  
      
      # ddl-auto: validate
      # trocar para validate depois de rodar pela primeira vez, caso contrário o banco de dados sempre será resetado